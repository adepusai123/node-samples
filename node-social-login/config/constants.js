//ENVIRONMENT Variable will select config file according to config-{ENVIRONMENT}.js, so if ENVIRONMENT: 'dev' then it will pick config-dev.json. 
module.exports = {
    ENVIRONMENT: 'dev', //dev or local
};